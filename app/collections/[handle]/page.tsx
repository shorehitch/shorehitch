import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import{notFound}from"next/navigation";import SiteShell from "../../../components/storefront/site-shell";import{getCollection}from"../../../lib/shopify/products";export const revalidate=900;function money(a:string,c:string){return new Intl.NumberFormat("en-US",{style:"currency",currency:c}).format(Number(a))}export async function generateMetadata({params}:{params:Promise<{handle:string}>}):Promise<Metadata>{const{handle}=await params,c=await getCollection(handle);if(!c)return{title:"Collection Not Found"};return{title:c.seo.title||c.title,description:c.seo.description||c.description,alternates:{canonical:`/collections/${c.handle}`}}}export default async function CollectionPage({params}:{params:Promise<{handle:string}>}){const{handle}=await params,c=await getCollection(handle);if(!c)notFound();const siteUrl=process.env.NEXT_PUBLIC_SITE_URL||"https://shorehitch.com",json={"@context":"https://schema.org","@type":"ItemList",name:c.title,itemListElement:c.products.nodes.map((p,i)=>({"@type":"ListItem",position:i+1,name:p.title,url:`${siteUrl}/products/${p.handle}`}))};return <SiteShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(json).replace(/</g,"\\u003c")}}/><section className="relative border-b border-white/10 bg-[#070707]"><div className="absolute inset-0 bg-[radial-gradient(circle_at_80%_15%,rgba(74,201,211,.11),transparent_30%)]"/><div className="relative mx-auto max-w-7xl px-5 py-16 md:px-8 md:py-20"><div className="text-xs font-black uppercase tracking-[.22em] text-[#4AC9D3]">ShoreHitch collection</div><h1 className="mt-3 text-4xl font-black tracking-tight md:text-6xl">{c.title}</h1>{c.description&&<p className="mt-5 max-w-3xl text-base leading-7 text-white/58">{c.description}</p>}</div></section><section className="mx-auto max-w-7xl px-5 py-14 md:px-8"><div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{c.products.nodes.map(p=><Link key={p.id} href={`/products/${p.handle}`} className="product-card group overflow-hidden rounded-xl border border-white/10 bg-[#0A0A0A] hover:border-[#4AC9D3]/50"><div className="aspect-square overflow-hidden bg-[#111]">{p.featuredImage?<Image src={p.featuredImage.url} alt={p.featuredImage.altText||p.title} width={p.featuredImage.width||1000} height={p.featuredImage.height||1000} sizes="(max-width:640px) 100vw,(max-width:1024px) 50vw,33vw" className="premium-media h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]"/>:null}</div><div className="p-5"><h2 className="text-xl font-bold">{p.title}</h2><div className="mt-4 font-black">{money(p.priceRange.minVariantPrice.amount,p.priceRange.minVariantPrice.currencyCode)}</div></div></Link>)}</div></section></SiteShell>}
