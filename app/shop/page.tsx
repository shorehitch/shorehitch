import type { Metadata } from "next";import SiteShell from "../../components/storefront/site-shell";import ProductCard from"../../components/storefront/product-card";import{getProducts}from"../../lib/shopify/products";export const revalidate=900;export const metadata:Metadata={title:"Shop Anchoring Systems & Marine Accessories",description:"Shop ShoreHitch anchoring systems, dock lines, tether adjusters, swivels, storage and customization options for boating and outdoor use.",alternates:{canonical:"/shop"}};export default async function ShopPage(){const products=await getProducts(50).catch(()=>[]),siteUrl=process.env.NEXT_PUBLIC_SITE_URL||"https://shorehitch.com",itemListJsonLd={"@context":"https://schema.org","@type":"ItemList",name:"ShoreHitch Products",itemListElement:products.map((p,i)=>({"@type":"ListItem",position:i+1,name:p.title,url:`${siteUrl}/products/${p.handle}`}))};return <SiteShell><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(itemListJsonLd).replace(/</g,"\\u003c")}}/><section className="mx-auto max-w-7xl px-5 py-14 md:px-8 md:py-16"><div className="mb-10"><div className="mb-3 flex items-center gap-3"><span className="h-px w-6 bg-[#4AC9D3]"/><span className="text-xs font-semibold uppercase tracking-[.2em] text-[#4AC9D3]">Full Catalog</span></div><h1 className="text-4xl font-bold tracking-tight md:text-5xl">The Collection</h1><p className="mt-3 max-w-xl text-base leading-7 text-white/60">Every product built from real on-water experience. ShoreHitch anchor units are backed by a lifetime warranty.</p></div><div className="mb-10 flex flex-wrap gap-4 rounded-lg border border-white/10 bg-[#0A0A0A] px-5 py-3 text-sm text-white/60"><span>🇺🇸 Designed in USA</span><span>🛡️ Lifetime Warranty on Anchors</span><span>Secure Shopify checkout</span></div>{products.length?<div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">{products.map((p,i)=><ProductCard key={p.id} product={p} priority={i<3}/>)}</div>:<div className="rounded-xl border border-white/10 bg-[#0A0A0A] px-6 py-16 text-center"><h2 className="text-2xl font-black">Catalog temporarily refreshing.</h2><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-white/55">Our Shopify catalog connection is taking longer than expected. Refresh shortly to load the current products, pricing and availability.</p></div>}</section></SiteShell>}
